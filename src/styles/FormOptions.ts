import { css } from '@emotion/css'
import theme from './_.theme';

export default {
  h4: css({
    color: theme.base0B,
    fontSize: '14px',
    margin: '0',
    padding: '20px 0 5px 20px',
  }),
};
